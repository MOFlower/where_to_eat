syntax = "proto3";

option go_package = "where_to_eat/network/protobuf";
package network;

service W2E {
    rpc  BroadCast(Req) returns (Resp) {}
};

message Req {
    int64 clientId = 1;
    string msg = 2;
};

message Resp {
    int64 toClientId = 1;
    string msg = 2;
};